#pragma once
#include "ColorRGB.h"

const ColorRGB black(0.0);
const ColorRGB white(1.0);
const ColorRGB red(1.0, 0.0, 0.0);
const ColorRGB blue(0.0, 0.0, 1.0);

const double 	kEpsilon = 0.0001;
const double	kHugeValue = 1.0E10;